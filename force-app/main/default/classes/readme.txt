go to external client app --> external client app manager ---> new external client app

Name do  and Oauth on karo 

callurl ---> authentication karne ke baad kidhr jaana hai // after token where u want to go 

base url/services/oauth/success
https://login.salesforce.com/services/oauth2/success


oauth scope 

Full access (full)
Manage user data via APIs (api)
Perform requests at any time (refresh_token